services:
  ai-ad-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-ad-generator
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
